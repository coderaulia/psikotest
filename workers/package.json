{
  "name": "psikotest-api-workers",
  "version": "2.2.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "migrate:local": "for file in ./migrations/*.sql; do wrangler d1 execute psikotest-db --local --file=\"$file\"; done",
    "migrate:remote": "for file in ./migrations/*.sql; do wrangler d1 execute psikotest-db --file=\"$file\"; done"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.57.0"
  }
}
